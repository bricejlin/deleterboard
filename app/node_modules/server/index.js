'use strict';

require('node-jsx').install({ extension: '.jsx' });

var express = require('express');

var app = express();

var pubPath = __dirname + '/../public';

app.use(function (req, res, next) {
  console.log("YOOOOOOOOOO", req.url);
  next();
});

// Serve static files
app.use(express.static(pubPath));

require('routes')(app);

module.exports = app;
